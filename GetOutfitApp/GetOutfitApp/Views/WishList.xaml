<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GetOutfitApp.Views.WishList" 
             
             xmlns:vm="clr-namespace:GetOutfitApp.ViewModel"
            Title="GetOutfit"
            >
  <!--Pages can be added as references or inline-->

    <ContentPage.BindingContext>
        <vm:WishListViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding  ItemsCart}"
                            VerticalOptions="FillAndExpand"
                            VerticalScrollBarVisibility="Never"
                        
                            ItemsLayout="VerticalList"
                            >
            <CollectionView.ItemTemplate >

                <DataTemplate>
                    <Grid>
                        <Label  Text="{Binding  Name}" HorizontalOptions="Center" Style="{StaticResource BlackLabel}">
                        </Label>

                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </ContentPage.Content>
</ContentPage>